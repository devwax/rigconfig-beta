/*
import { Meteor } from 'meteor/meteor';
import { ValidatedMethod } from 'meteor/mdg:validated-method';
import SimpleSchema from 'simpl-schema';
// import { ComponentCollectionNames } from '/imports/api/lib/ComponentTypes.js';
import { cpus } from '/imports/api/official_components/cpus/cpus.js';
import { motherboards } from '/imports/api/official_components/motherboards/motherboards.js';

export const getRigComponents = new ValidatedMethod({
  name: 'lib.getRigComponents',
  validate: new SimpleSchema({
    sourceRigComponentIds: { type: [String], max: 30 }
  }).validator(),
  run({sourceRigComponentIds}) {
    // 1. fetch the data for each of them in the official_components collection (or individual collections)
    // 2. return the components list
    // check to see if we can get this from the appstate results or subscription

  }
})
*/
