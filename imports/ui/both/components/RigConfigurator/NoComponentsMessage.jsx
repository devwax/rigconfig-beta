import { Meteor } from 'meteor/meteor';
import React from "react";

export default () =>
  <div id="NoComponentsMessage">
    <p>
      <strong>No Components...</strong>
    </p>
    <p>Add components using the 'Add to Rig' button or add a custom component with the <em>Add Component</em> button below.</p>
  </div>
